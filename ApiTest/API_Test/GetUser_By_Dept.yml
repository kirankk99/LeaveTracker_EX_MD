info:
  name: GetUser_By_Dept
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:5000/api/users/by-department?deptName=Engineering&role=MANAGER
  params:
    - name: deptName
      value: Engineering
      type: query
    - name: role
      value: MANAGER
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​